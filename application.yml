spring.datasource.url: jdbc:${CLEARDB_DATABASE_URL:mysql://localhost:3306/springbootapp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}

spring.jpa.hibernate.ddl-auto: none
spring.jpa.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect

spring.flyway.baselineOnMigrate : true
spring.flyway.validateOnMigrate : false
spring.jpa.show-sql: false
spring.jpa.hibernate.globally_quoted_identifiers: true

spring.servlet.multipart.max-file-size: -1
spring.servlet.multipart.max-request-size: -1
server.port: ${PORT:8080}

spring:
  mail:
    host: smtp.yandex.ru
    username: springrobot
    password: ${MAIL_PASSWORD}
    port: 465
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true


blog.title: ITsociety
blog.subtitle: Рассказы разработчиков
blog.phone: +373 686-544-17
blog.email: andreiciugreanu7@mail.com
blog.copyright: Андрей Чугурну
blog.copyrightFrom: 2020

blog.mailbot.password: ${MAIL_PASSWORD}
blog.mailbot.username: springrobot